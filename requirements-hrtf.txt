# Optional standards verification and advanced HRTF processing dependencies.
# Basic .sofa loading is part of requirements.txt (via h5py) because the GUI
# exposes the Phase 4 asset loader in a standard installation.
-r requirements.txt
sofar>=1.1.3
pyfar>=0.7
